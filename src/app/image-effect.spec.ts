import { ImageEffect } from './image-effect';

describe('ImageEffect', () => {
  it('should create an instance', () => {
    const directive = new ImageEffect();
    expect(directive).toBeTruthy();
  });
});
